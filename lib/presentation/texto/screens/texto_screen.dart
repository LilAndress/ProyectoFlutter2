
import 'package:flutter/material.dart';

class TextoScreen extends StatelessWidget {
  const TextoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(appBar:  AppBar(title: Text("Hola texto")));
  }
}